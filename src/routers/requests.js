const { userAuth } = require('../middlewares/auth');
const express = require('express');

const requestsRouter = express.Router();

// Add your requests routes here

module.exports = requestsRouter;

